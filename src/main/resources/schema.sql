/*

// You can create script or use java classes with anotation @Entity
CREATE TABLE tableName (
    id          INTEGER      NOT NULL AUTO_INCREMENT,
    name        VARCHAR(128) NOT NULL,
    population  INTEGER NOT NULL,
    PRIMARY KEY (id)
);

*/